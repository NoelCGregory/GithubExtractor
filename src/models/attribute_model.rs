pub struct Attribute {
    pub number_constant: i32,
    pub number_parser: i32,
    pub number_declaration: i32,
    pub number_control: i32,
    pub number_if: i32,
    pub error_array: Vec<String>,
}
